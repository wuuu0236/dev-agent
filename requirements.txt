# --- LLM API ---
openai>=1.0.0
python-dotenv>=1.0.0

# --- Agent 框架 ---
# 锁 <1.0：ragas 0.4.x 基于 langchain 0.3 生态编写，装 1.x 会崩（vertexai 模块被拆走）
langgraph>=0.2.0,<1.0
langchain-core>=0.3.0,<1.0
langchain-openai>=0.2.0,<1.0
# Langfuse 的 langchain 集成依赖 langchain 本体（否则 dev_agent_langgraph 导入即崩）
langchain>=0.3.0,<1.0

# --- Web 前端（DataLens） ---
streamlit>=1.28.0

# --- 向量检索 ---
chromadb>=0.4.0
numpy>=1.24.0

# --- 混合检索 ---
rank-bm25>=0.2.2
jieba>=0.42.0

# --- 文档解析 ---
PyMuPDF>=1.23.0
python-docx>=1.0.0

# --- 多模态 OCR（本地、纯 CPU、数据不出域）---
# 版本下限放宽到 1.2.x：1.3.x 全系要求 Python <3.13，会卡死 3.13 环境安装
rapidocr_onnxruntime>=1.2.0
pillow>=10.0.0

# --- API 服务 ---
fastapi>=0.100.0
uvicorn>=0.20.0
# 锁 mcp 1.x：代码用 from mcp.server.fastmcp import FastMCP，2.0 移除了该 API
mcp>=1.0.0,<2.0.0

# --- 评估 ---
pandas>=2.0.0
# 业界标准 RAG 评测框架（指标引擎）
ragas>=0.4.0,<0.5

# --- 可观测性 ---
# 锁 2.50.x：只依赖 @observe / callback.langchain，2.50 验证可用；
# 不用 langfuse.openai（部分环境导入即崩 + 全局 patch openai 干扰 RAGAS）
langfuse>=2.50.0,<2.51.0

