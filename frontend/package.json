{
  "name": "datalens-react-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "DataLens React 前端（单文件打包，零外部依赖，双击 index.html 即可打开）",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
