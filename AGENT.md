# AGENT.md — 本项目协作规则

给所有在本仓库工作的 AI 助手 / 编程 Agent 看的规则。优先级高于你的默认行为习惯。

## 1. 只做我明确要求的事（最高优先级）

**严格按指令范围动手，不做任何超出范围的事。**

- 我说「改 A」，就只改 A；不顺手重构 B、不顺手格式化 C、不顺手补 D。
- 不要主动加功能、加参数、加注释、加测试、加文档，除非我明确说了要。
- 不要「顺手优化」：重命名变量、调整目录结构、统一代码风格、补类型标注、加错误处理——这些都要先问我。
- 不要主动执行额外命令（构建、测试、格式化、依赖安装），除非该命令是完成我所要求事项的必要步骤。

## 2. 发现别的问题：报告，但不许动

你在本项目里**看到**其他问题、bug、坏味道、过时文档、缺失依赖、安全隐患——**只报告，不修改**。

正确做法：在回复末尾单列一段「顺带发现（未处理）」，写清：

- 位置（文件:行号）
- 是什么问题
- 为什么你觉得需要处理
- 建议怎么改

然后**停下来等我决定**。我不会因为你没动手而认为你失职，恰恰相反——擅自改动才算失职。

## 3. 指令有歧义时：先问，不要猜

- 需求有多种合理解法 → 问清楚再动手，不要替我选。
- 需要的参数/路径/分支名没给 → 问，不要凭印象猜（尤其是账号名、仓库 owner、环境变量名这类硬事实）。
- 做到一半发现原方案行不通 → 停下来说明，不要自行换方案继续做。

## 4. 对外动作必须先确认

以下动作**必须先问我**，得到明确答复才执行：

- `git push` / 发 PR / 发 issue / 提 comment
- 发消息、发邮件、提交表单
- 删除文件、删除分支、覆盖他人提交（`--force`）
- 安装全局依赖、修改系统配置

例外：我在本次对话里已经明确授权过（例如「先 push 吧」），则该次可直接执行。

## 5. 改动的边界

- 能用编辑解决就不要重写文件；能改一处就不要改一片。
- 提交信息如实描述改动，不夸大、不隐藏副作用。
- 一次提交只做一件事；我要求的多件事可以分开提交，但要告诉我每提交在干什么。

## 6. 汇报要求

完成后简要说明：改了哪些文件、每个文件改了什么、有没有产生副作用、有没有（未处理的）顺带发现。
不要把工具原始输出整段贴给我，提炼结论即可。
